# Skills Setup

## Structure

- marketingskills bundle (install via: `npx skills add coreyhaines31/marketingskills`)
  - installs many marketing skill folders directly under `.agents/skills/` (for example: `ad-creative/`, `seo-audit/`, `social/`)
- stop-slop/ (clone via: `git clone hardikpandya/stop-slop`)
- ui-ux-pro-max/ (install via: `npm install -g uipro-cli && uipro init --ai claude`)
- content-research-writer/ (source: `ComposioHQ/awesome-claude-skills/content-research-writer`)

## Notes

- remotion-video is intentionally excluded from this repository due to security concerns.
